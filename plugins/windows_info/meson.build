# Windows Info Plugin
# Only available on Windows

set_variable('plugin_name', 'windows_info')
set_variable('plugin_sources', files('windows_info.cpp'))
set_variable('plugin_deps', [])
set_variable('plugin_platform', 'windows') # 'all' for cross-platform, or specific platform name
